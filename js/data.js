const generatedCosmos = Array.from({ length: 13 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/cosmos${id}.jpg`,
    alt: `Космос Десктоп ${id}`,
    category: "Desktop",
    tag: "space",
  };
});

const generatedNature = Array.from({ length: 20 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/nature${id}.jpg`,
    alt: `Природа Десктоп ${id}`,
    category: "Desktop",
    tag: "nature",
  };
});

const generatedDark = Array.from({ length: 11 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/dark${id}.jpg`,
    alt: `Темные Десктоп ${id}`,
    category: "Desktop",
    tag: "black",
  };
});

const generatedWhite = Array.from({ length: 11 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/white${id}.jpg`,
    alt: `Светлые Десктоп ${id}`,
    category: "Desktop",
    tag: "white",
  };
});

const generatedSky = Array.from({ length: 13 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/sky${id}.jpg`,
    alt: `Небо Десктоп ${id}`,
    category: "Desktop",
    tag: "sky",
  };
});

const generatedCity = Array.from({ length: 20 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/city${id}.jpg`,
    alt: `Город Десктоп ${id}`,
    category: "Desktop",
    tag: "city",
  };
});

const generatedMinimalism = Array.from({ length: 16 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/min${id}.jpg`,
    alt: `Минимализм Десктоп ${id}`,
    category: "Desktop",
    tag: "minimalism",
  };
});

const generatedNaturePhone = Array.from({ length: 25 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/nature${id}.jpg`,
    alt: `Минимализм Мобильные ${id}`,
    category: "Phone",
    tag: "nature",
  };
});

const generatedCosmosPhone = Array.from({ length: 25 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/cosmos${id}.jpg`,
    alt: `Космос Мобильные ${id}`,
    category: "Phone",
    tag: "space",
  };
});

const generatedDarkPhone = Array.from({ length: 11 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/dark${id}.jpg`,
    alt: `Темные Мобильные ${id}`,
    category: "Phone",
    tag: "black",
  };
});

const generatedWhitePhone = Array.from({ length: 11 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/white${id}.jpg`,
    alt: `Светлые Мобильные ${id}`,
    category: "Phone",
    tag: "white",
  };
});

const generatedMinPhone = Array.from({ length: 12 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/min${id}.jpg`,
    alt: `Минимализм Мобильные ${id}`,
    category: "Phone",
    tag: "minimalism",
  };
});

const generatedSkyPhone = Array.from({ length: 11 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/sky${id}.jpg`,
    alt: `Небо Мобильные ${id}`,
    category: "Phone",
    tag: "sky",
  };
});
const generatedCityPhone = Array.from({ length: 10 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/city${id}.jpg`,
    alt: `Город Мобильные ${id}`,
    category: "Phone",
    tag: "city",
  };
});
const generatedCarPhone = Array.from({ length: 10 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/phone/cards/car${id}.jpg`,
    alt: `Машины Мобильные ${id}`,
    category: "Phone",
    tag: "car",
  };
});

const generatedCar = Array.from({ length: 5 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/desktop/cards_min_size/car${id}.jpg`,
    alt: `Машины Десктоп ${id}`,
    category: "Desktop",
    tag: "car",
  };
});

const generatedTendies = Array.from({ length: 17 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/ios/blocks/${id}.webp`,
    alt: `Tendies iOS ${id}`,
    category: "iOS (tendies)",
  };
});

const generatedWallEng = Array.from({ length: 6 }, (_, i) => {
  const id = i + 1;
  return {
    src: `https://raw.githubusercontent.com/splatedk-web/wallpaper-vault/main/images/wp/cards/${id}.webp`,
    alt: `WallPaper Engine ${id}`,
    category: "Wallpaper Engine",
  };
});

window.wallpapersData = [
  ...generatedCosmos,
  ...generatedNature,
  ...generatedCity,
  ...generatedDark,
  ...generatedWhite,
  ...generatedSky,
  ...generatedCar,
  ...generatedMinimalism,
  ...generatedNaturePhone,
  ...generatedCosmosPhone,
  ...generatedDarkPhone,
  ...generatedWhitePhone,
  ...generatedMinPhone,
  ...generatedCityPhone,
  ...generatedSkyPhone,
  ...generatedCarPhone,
  ...generatedTendies,
  ...generatedWallEng,
];
